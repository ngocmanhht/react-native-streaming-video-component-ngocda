///
/// HybridVideoPlayerViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridVideoPlayerViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::streamingvideo::views {

  extern const char HybridVideoPlayerViewComponentName[] = "VideoPlayerView";

  HybridVideoPlayerViewProps::HybridVideoPlayerViewProps(const react::PropsParserContext& context,
                                                         const HybridVideoPlayerViewProps& sourceProps,
                                                         const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    url([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("url", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.url;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.url);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.url: ") + exc.what());
      }
    }()),
    streamProtocol([&]() -> CachedProp<StreamProtocol> {
      try {
        const react::RawValue* rawValue = rawProps.at("streamProtocol", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.streamProtocol;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<StreamProtocol>::fromRawValue(*runtime, value, sourceProps.streamProtocol);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.streamProtocol: ") + exc.what());
      }
    }()),
    paused([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("paused", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.paused;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.paused);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.paused: ") + exc.what());
      }
    }()),
    resizeMode([&]() -> CachedProp<ResizeMode> {
      try {
        const react::RawValue* rawValue = rawProps.at("resizeMode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.resizeMode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<ResizeMode>::fromRawValue(*runtime, value, sourceProps.resizeMode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.resizeMode: ") + exc.what());
      }
    }()),
    volume([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("volume", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.volume;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.volume);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.volume: ") + exc.what());
      }
    }()),
    muted([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("muted", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.muted;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.muted);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.muted: ") + exc.what());
      }
    }()),
    shouldRepeat([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("shouldRepeat", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.shouldRepeat;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.shouldRepeat);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.shouldRepeat: ") + exc.what());
      }
    }()),
    progressInterval([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("progressInterval", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.progressInterval;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.progressInterval);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.progressInterval: ") + exc.what());
      }
    }()),
    zoomEnabled([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("zoomEnabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.zoomEnabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.zoomEnabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.zoomEnabled: ") + exc.what());
      }
    }()),
    onReady([&]() -> CachedProp<std::optional<std::function<void(const ReadyEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onReady", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onReady;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const ReadyEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onReady);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.onReady: ") + exc.what());
      }
    }()),
    onProgress([&]() -> CachedProp<std::optional<std::function<void(const ProgressEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onProgress", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onProgress;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const ProgressEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onProgress);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.onProgress: ") + exc.what());
      }
    }()),
    onBuffering([&]() -> CachedProp<std::optional<std::function<void(bool /* isBuffering */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onBuffering", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onBuffering;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(bool /* isBuffering */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onBuffering);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.onBuffering: ") + exc.what());
      }
    }()),
    onStateChange([&]() -> CachedProp<std::optional<std::function<void(PlaybackState /* state */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onStateChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onStateChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(PlaybackState /* state */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onStateChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.onStateChange: ") + exc.what());
      }
    }()),
    onError([&]() -> CachedProp<std::optional<std::function<void(const ErrorEvent& /* event */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onError", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onError;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const ErrorEvent& /* event */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onError);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.onError: ") + exc.what());
      }
    }()),
    onEnd([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onEnd", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onEnd;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onEnd);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.onEnd: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridVideoPlayerViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridVideoPlayerViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VideoPlayerView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridVideoPlayerViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("url"): return true;
      case hashString("streamProtocol"): return true;
      case hashString("paused"): return true;
      case hashString("resizeMode"): return true;
      case hashString("volume"): return true;
      case hashString("muted"): return true;
      case hashString("shouldRepeat"): return true;
      case hashString("progressInterval"): return true;
      case hashString("zoomEnabled"): return true;
      case hashString("onReady"): return true;
      case hashString("onProgress"): return true;
      case hashString("onBuffering"): return true;
      case hashString("onStateChange"): return true;
      case hashString("onError"): return true;
      case hashString("onEnd"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridVideoPlayerViewComponentDescriptor::HybridVideoPlayerViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridVideoPlayerViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                           const std::shared_ptr<const react::Props>& props,
                                                                                           react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridVideoPlayerViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridVideoPlayerViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridVideoPlayerViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridVideoPlayerViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridVideoPlayerViewProps>& props = std::const_pointer_cast<HybridVideoPlayerViewProps>(constProps);
    HybridVideoPlayerViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::streamingvideo::views
