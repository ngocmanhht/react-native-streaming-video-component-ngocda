///
/// PlaybackState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::streamingvideo {

  /**
   * An enum which can be represented as a JavaScript union (PlaybackState).
   */
  enum class PlaybackState {
    IDLE      SWIFT_NAME(idle) = 0,
    LOADING      SWIFT_NAME(loading) = 1,
    READY      SWIFT_NAME(ready) = 2,
    PLAYING      SWIFT_NAME(playing) = 3,
    PAUSED      SWIFT_NAME(paused) = 4,
    BUFFERING      SWIFT_NAME(buffering) = 5,
    ERROR      SWIFT_NAME(error) = 6,
    ENDED      SWIFT_NAME(ended) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::streamingvideo

namespace margelo::nitro {

  // C++ PlaybackState <> JS PlaybackState (union)
  template <>
  struct JSIConverter<margelo::nitro::streamingvideo::PlaybackState> final {
    static inline margelo::nitro::streamingvideo::PlaybackState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"): return margelo::nitro::streamingvideo::PlaybackState::IDLE;
        case hashString("loading"): return margelo::nitro::streamingvideo::PlaybackState::LOADING;
        case hashString("ready"): return margelo::nitro::streamingvideo::PlaybackState::READY;
        case hashString("playing"): return margelo::nitro::streamingvideo::PlaybackState::PLAYING;
        case hashString("paused"): return margelo::nitro::streamingvideo::PlaybackState::PAUSED;
        case hashString("buffering"): return margelo::nitro::streamingvideo::PlaybackState::BUFFERING;
        case hashString("error"): return margelo::nitro::streamingvideo::PlaybackState::ERROR;
        case hashString("ended"): return margelo::nitro::streamingvideo::PlaybackState::ENDED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum PlaybackState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::streamingvideo::PlaybackState arg) {
      switch (arg) {
        case margelo::nitro::streamingvideo::PlaybackState::IDLE: return JSIConverter<std::string>::toJSI(runtime, "idle");
        case margelo::nitro::streamingvideo::PlaybackState::LOADING: return JSIConverter<std::string>::toJSI(runtime, "loading");
        case margelo::nitro::streamingvideo::PlaybackState::READY: return JSIConverter<std::string>::toJSI(runtime, "ready");
        case margelo::nitro::streamingvideo::PlaybackState::PLAYING: return JSIConverter<std::string>::toJSI(runtime, "playing");
        case margelo::nitro::streamingvideo::PlaybackState::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::streamingvideo::PlaybackState::BUFFERING: return JSIConverter<std::string>::toJSI(runtime, "buffering");
        case margelo::nitro::streamingvideo::PlaybackState::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        case margelo::nitro::streamingvideo::PlaybackState::ENDED: return JSIConverter<std::string>::toJSI(runtime, "ended");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert PlaybackState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"):
        case hashString("loading"):
        case hashString("ready"):
        case hashString("playing"):
        case hashString("paused"):
        case hashString("buffering"):
        case hashString("error"):
        case hashString("ended"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
