///
/// JHybridVideoPlayerViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridVideoPlayerViewStateUpdater.hpp"
#include "views/HybridVideoPlayerViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::streamingvideo::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridVideoPlayerViewState>;

void JHybridVideoPlayerViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridVideoPlayerViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridVideoPlayerViewSpec> hybridView = javaView->getJHybridVideoPlayerViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridVideoPlayerViewState& data = concreteState->getData();
  const std::shared_ptr<HybridVideoPlayerViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridVideoPlayerViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->url.isDirty) {
    hybridView->setUrl(props->url.value);
    props->url.isDirty = false;
  }
  if (props->streamProtocol.isDirty) {
    hybridView->setStreamProtocol(props->streamProtocol.value);
    props->streamProtocol.isDirty = false;
  }
  if (props->paused.isDirty) {
    hybridView->setPaused(props->paused.value);
    props->paused.isDirty = false;
  }
  if (props->resizeMode.isDirty) {
    hybridView->setResizeMode(props->resizeMode.value);
    props->resizeMode.isDirty = false;
  }
  if (props->volume.isDirty) {
    hybridView->setVolume(props->volume.value);
    props->volume.isDirty = false;
  }
  if (props->muted.isDirty) {
    hybridView->setMuted(props->muted.value);
    props->muted.isDirty = false;
  }
  if (props->shouldRepeat.isDirty) {
    hybridView->setShouldRepeat(props->shouldRepeat.value);
    props->shouldRepeat.isDirty = false;
  }
  if (props->progressInterval.isDirty) {
    hybridView->setProgressInterval(props->progressInterval.value);
    props->progressInterval.isDirty = false;
  }
  if (props->zoomEnabled.isDirty) {
    hybridView->setZoomEnabled(props->zoomEnabled.value);
    props->zoomEnabled.isDirty = false;
  }
  if (props->onReady.isDirty) {
    hybridView->setOnReady(props->onReady.value);
    props->onReady.isDirty = false;
  }
  if (props->onProgress.isDirty) {
    hybridView->setOnProgress(props->onProgress.value);
    props->onProgress.isDirty = false;
  }
  if (props->onBuffering.isDirty) {
    hybridView->setOnBuffering(props->onBuffering.value);
    props->onBuffering.isDirty = false;
  }
  if (props->onStateChange.isDirty) {
    hybridView->setOnStateChange(props->onStateChange.value);
    props->onStateChange.isDirty = false;
  }
  if (props->onError.isDirty) {
    hybridView->setOnError(props->onError.value);
    props->onError.isDirty = false;
  }
  if (props->onEnd.isDirty) {
    hybridView->setOnEnd(props->onEnd.value);
    props->onEnd.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::streamingvideo::views
