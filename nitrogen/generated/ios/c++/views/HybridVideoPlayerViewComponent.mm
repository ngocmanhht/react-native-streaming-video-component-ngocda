///
/// HybridVideoPlayerViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridVideoPlayerViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridVideoPlayerViewSpecSwift.hpp"
#import "react_native_streaming_video_component_ngocda-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::streamingvideo;
using namespace margelo::nitro::streamingvideo::views;

/**
 * Represents the React Native View holder for the Nitro "VideoPlayerView" HybridView.
 */
@interface HybridVideoPlayerViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridVideoPlayerViewComponent {
  std::shared_ptr<HybridVideoPlayerViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridVideoPlayerViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridVideoPlayerViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridVideoPlayerViewSpec> hybridView = react_native_streaming_video_component_ngocda::react_native_streaming_video_component_ngocdaAutolinking::createVideoPlayerView();
    _hybridView = std::dynamic_pointer_cast<HybridVideoPlayerViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  react_native_streaming_video_component_ngocda::HybridVideoPlayerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridVideoPlayerViewProps const>(props);
  auto& newViewProps = const_cast<HybridVideoPlayerViewProps&>(newViewPropsConst);
  react_native_streaming_video_component_ngocda::HybridVideoPlayerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // url: string
  if (newViewProps.url.isDirty) {
    swiftPart.setUrl(newViewProps.url.value);
    newViewProps.url.isDirty = false;
  }
  // streamProtocol: enum
  if (newViewProps.streamProtocol.isDirty) {
    swiftPart.setStreamProtocol(static_cast<int>(newViewProps.streamProtocol.value));
    newViewProps.streamProtocol.isDirty = false;
  }
  // paused: boolean
  if (newViewProps.paused.isDirty) {
    swiftPart.setPaused(newViewProps.paused.value);
    newViewProps.paused.isDirty = false;
  }
  // resizeMode: enum
  if (newViewProps.resizeMode.isDirty) {
    swiftPart.setResizeMode(static_cast<int>(newViewProps.resizeMode.value));
    newViewProps.resizeMode.isDirty = false;
  }
  // volume: number
  if (newViewProps.volume.isDirty) {
    swiftPart.setVolume(newViewProps.volume.value);
    newViewProps.volume.isDirty = false;
  }
  // muted: boolean
  if (newViewProps.muted.isDirty) {
    swiftPart.setMuted(newViewProps.muted.value);
    newViewProps.muted.isDirty = false;
  }
  // shouldRepeat: boolean
  if (newViewProps.shouldRepeat.isDirty) {
    swiftPart.setShouldRepeat(newViewProps.shouldRepeat.value);
    newViewProps.shouldRepeat.isDirty = false;
  }
  // progressInterval: number
  if (newViewProps.progressInterval.isDirty) {
    swiftPart.setProgressInterval(newViewProps.progressInterval.value);
    newViewProps.progressInterval.isDirty = false;
  }
  // zoomEnabled: boolean
  if (newViewProps.zoomEnabled.isDirty) {
    swiftPart.setZoomEnabled(newViewProps.zoomEnabled.value);
    newViewProps.zoomEnabled.isDirty = false;
  }
  // onReady: optional
  if (newViewProps.onReady.isDirty) {
    swiftPart.setOnReady(newViewProps.onReady.value);
    newViewProps.onReady.isDirty = false;
  }
  // onProgress: optional
  if (newViewProps.onProgress.isDirty) {
    swiftPart.setOnProgress(newViewProps.onProgress.value);
    newViewProps.onProgress.isDirty = false;
  }
  // onBuffering: optional
  if (newViewProps.onBuffering.isDirty) {
    swiftPart.setOnBuffering(newViewProps.onBuffering.value);
    newViewProps.onBuffering.isDirty = false;
  }
  // onStateChange: optional
  if (newViewProps.onStateChange.isDirty) {
    swiftPart.setOnStateChange(newViewProps.onStateChange.value);
    newViewProps.onStateChange.isDirty = false;
  }
  // onError: optional
  if (newViewProps.onError.isDirty) {
    swiftPart.setOnError(newViewProps.onError.value);
    newViewProps.onError.isDirty = false;
  }
  // onEnd: optional
  if (newViewProps.onEnd.isDirty) {
    swiftPart.setOnEnd(newViewProps.onEnd.value);
    newViewProps.onEnd.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return react_native_streaming_video_component_ngocda::react_native_streaming_video_component_ngocdaAutolinking::isVideoPlayerViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  react_native_streaming_video_component_ngocda::HybridVideoPlayerViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

@end
